/**
 * The `Forgotpassword` component is a simple functional component in React that renders a text saying
 * "Forgotpassword".
 * @returns The `Forgotpassword` component is being returned. It is a functional component that simply
 * renders a `<div>` element with the text "Forgotpassword".
 */
/**
 * The CheckPasswordPage component in React handles user input for password verification and login
 * functionality.
 * @returns The `CheckPasswordPage` component is being returned. It is a functional component that
 * contains a form for checking the password. The component includes input fields for entering the
 * password, a submit button for logging in, and a link for resetting the password. The component also
 * displays the user's name and profile picture using the `Avatar` component.
 */
import React from "react";

const Forgotpassword = () => {
  return <div>Forgotpassword</div>;
};

export default Forgotpassword;
