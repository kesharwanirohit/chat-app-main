/**
 * The `Divider` component in React returns a div element with specific styling for creating a visual
 * divider.
 * @returns A functional React component named Divider is being returned. This component renders a div
 * element with the class name 'p-[0.5px] bg-slate-200 my-1'.
 */
import React from "react";

const Divider = () => {
  return <div className="p-[0.5px] bg-slate-200 my-1"></div>;
};

export default Divider;
